---
description: Conventions for the wpis-bots repository (Mastodon + Bluesky, depends on wpis-core)
globs: packages/wpis-bots/**/*
alwaysApply: false
---

# WPIS Bots

- **Layout**: this repository **is** the plugin root (`wpis-bots.php`, `lib/` including `autoload-runtime.php`, `src/Mastodon/`, `src/Bluesky/`). Composer `vendor/` is for local dev (PHPCS, PHPUnit) and is gitignored. Shipped French admin markdown lives under **`docs/`** (`docs/guide-admin.md`, `docs/limites-api-et-bonnes-pratiques.md`, `docs/ressources.md`); a WordPress.org-style **`readme.txt`** (English) sits at the repo root. **`.doc/`** is gitignored like `wpis-core` / `wpis-theme`. `tests/` and `phpunit.xml` sit beside the plugin files for the repo only. **Action Scheduler** is not bundled; the plugin is declared in `Requires Plugins` alongside `wpis-core` (WordPress Is… Core).
- **Do not** reintroduce a nested `wpis-bots/wpis-bots/` plugin folder or split `wpis-bot-mastodon/` / `wpis-bot-bluesky/` as separate plugin roots without an explicit migration.
